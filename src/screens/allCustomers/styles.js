import { StyleSheet } from "react-native";
import { wp } from "../../../App";

export const AllCustomersListStyle = StyleSheet.create({
    addBtn : {
       alignSelf:'flex-end'
    },
    icon : {
        color: '#EEEEEE',
        fontWeight: '700'
    }
})